"""apps.editor — `ed`-style line editor in a compositor window.

Importing this package registers the ``editor`` app so
``pythonos_gui editor`` will launch it.
"""

from apps.editor import edwin  # noqa: F401
