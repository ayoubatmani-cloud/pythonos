"""
kernel.gui — Display, input, and window-management surface used by
``pythonos_gui`` and the ``sdl2`` compatibility shim.

Modules:
    input        — canonical asynchronous Event/EventQueue
    compositor   — stacking window manager (Phase 5)
    sdl2         — PySDL2-compatible Python API (Phase 4)
    image        — PNG/JPEG decoders (Phase 6)
"""
